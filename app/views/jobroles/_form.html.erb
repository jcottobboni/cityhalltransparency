<%= form_with(model: jobrole, local: true) do |form| %>
  <% if jobrole.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jobrole.errors.count, "error") %> prohibited this jobrole from being saved:</h2>

      <ul>
      <% jobrole.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
